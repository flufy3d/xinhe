# Persona unified (4B backbone) —— 引用 curriculum.yaml
base: qwen3.5-4b.yaml

curriculum_file: curriculum.yaml

training:
  grad_accum_steps: 1
  dtype: bfloat16

stage_overrides:
  default:
    batch_size: 8
  0_atomic_skeletons:
    batch_size: 12
  1_5beat_natural:
    batch_size: 12
  2_joint_consolidation:
    batch_size: 12
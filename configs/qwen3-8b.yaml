# ============================================================
# Xinhe — Qwen3-8B backbone (8B)
# ============================================================
base: base.yaml

backbone:
  type: "qwen"
  model_path: "./models/qwen3-8b"
  weights_path: ""
  hidden_size: 4096
  freeze: true

state:
  state_dim: 4096

lora:
  rank: 8
  alpha: 16
  target_modules: ["q_proj", "v_proj"]

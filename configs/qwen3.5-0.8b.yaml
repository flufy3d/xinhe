# Xinhe — Qwen3.5-0.8B backbone (6 层标准 attention + 18 层线性 attention)
# LoRA 配置统一在 base.yaml,这里只声明 backbone 路径。
base: base.yaml

backbone:
  type: "qwen"
  model_path: "./models/qwen3.5-0.8b"
  weights_path: ""
  hidden_size: 1024
  freeze: true

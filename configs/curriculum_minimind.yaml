# ============================================================
# 心核课程学习 — MiniMind 64M
#
# 用法:
#   python scripts/train.py --config configs/curriculum_minimind.yaml
# ============================================================
base: minimind.yaml
curriculum_file: curriculum.yaml

stage_overrides:
  default:
    batch_size: 16

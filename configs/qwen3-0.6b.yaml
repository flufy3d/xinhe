# ============================================================
# Xinhe — Qwen3-0.6B backbone (600M)
# ============================================================
base: base.yaml

backbone:
  type: "qwen"
  model_path: "./models/qwen3-0.6b"
  weights_path: ""
  hidden_size: 1024
  freeze: true

state:
  state_dim: 1024

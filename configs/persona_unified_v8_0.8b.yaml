# Persona unified v8 (0.8B backbone) —— 引用 curriculum_v8.yaml
base: qwen3.5-0.8b.yaml

curriculum_file: curriculum_v8.yaml

training:
  batch_size: 4
  grad_accum_steps: 1
  dtype: bfloat16

# 硬件相关 stage 覆盖
stage_overrides:
  default:
    batch_size: 4

# Persona unified (0.8B backbone) —— 引用 curriculum.yaml
base: qwen3.5-0.8b.yaml

curriculum_file: curriculum.yaml

training:
  grad_accum_steps: 1
  dtype: bfloat16

stage_overrides:
  default:
    batch_size: 14
  0_atomic_skeletons:
    batch_size: 16
  1_5beat_natural:
    batch_size: 14
  2_joint_consolidation:
    batch_size: 14